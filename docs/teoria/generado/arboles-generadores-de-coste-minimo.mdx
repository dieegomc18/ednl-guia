---
title: "Árboles generadores de coste mínimo"
---
import useBaseUrl from '@docusaurus/useBaseUrl';

> Fuente: <a href={useBaseUrl('/ednl/TeoriaEDNL.pdf')} data-noBrokenLinkCheck>TeoriaEDNL.pdf</a>

10. ´ Arboles generadores de coste m´ ıni- mo En este cap´ ıtulo vamos a conocer algunos algoritmos (TAD partici´ on, Kruskal o Prim) para obtener grafos conexos ac´ ıclico , es decir un ´ arbol. Es decir, dado un grafo no dirigido y conexo G = ( V, A ), se define un ´ arbol generador de G (o de expansi´ on) como un ´ arbol que conecta todos los v´ ertices de G sin que haya ciclos. Su coste es la suma de los costes de las aristas del grafo. Figura 10.1: Ejemplos de ´ arboles generadores de coste m´ ınimo 10.1. TAD Partici´ on Una partici´ on es de un conjunto de enteros C = \{ 0 , 1 , ..., n − 1 \} es un conjunto de subcon- juntos disjuntos, cuya uni´ on es el conjunto total C . Encontramos la enumeraci´ on de elementos → Definimos una aplicaci´ on biyectiva (1a1) de un conjunto C en un rango de enteros [0 , n − 1], tal que n es el cardinal de C , mediante dos funciones: int IndiceElto(tElemento x); Precondici´ on: x ∈ C . Postcondici´ on: Devuelve el ´ ındice del elemento x, en el rango [0 , n − 1]. tElemento NombreElto(int i); Precondici´ on: 0 ≤ i ≤ n − 1. Postcondici´ on: Devuelve el elemento de C cuyo ´ ındice es i. 62

10.1.1. Especificaci´ on del TAD Partici´ on Operaciones del TAD partici´ on: Constructor de la partici´ on Postcondici´ on: Crea una partici´ on de subconjuntos unitarios en el intervalo de enteros 0 , n − 1. Partici´ on (int n); Unir subconjuntos Precondici´ on: 0 ≤ a , b ≤ n − 1, a y b son los representantes de sus clases y a ̸ = b . Postcondici´ on: Une el subconjunto del elemento a y b en uno de los dos subconjuntos. La partici´ on queda con un miembro menos. void unir(int a, int b); Buscar el representante de una partici´ on Precondici´ on: 0 ≤ x ≤ n − 1. Postcondici´ on: Devuelve el representante del subconjunto al que pertenece x . int encontrar(int x) const; 10.1.2. Implementaci´ on del TAD Partici´ on El TAD partici´ on podemos implementarlo de diversas maneras (vector, listas, ´ arboles,..), por tanto, vamos a ver como se implementan los algoritmos anteriores. Implementaci´ on mediante vector de pertenencia En cada posici´ on del vector vamos a encontrar el representante de cada subconjunto. Figura 10.2: Ejemplo implementaci´ on vector de pertenencia Vemos que en el subconjunto \{ 0 , 4 \} , en la posici´ on ‘ 0 ’ del vector, econtramos el representante ‘ 4 ’, es decir, cada elemento en negrita es el representante del subconjunto. Si queremos unir dos subconjuntos, primero llamaremos al m´ etodo encontrar() y luego al m´ etodo unir() . El coste de las operaciones son: encontrar() ∈ O (1), porque accedemos a la posici´ on directamente. unir() ∈ O ( n ), porque recorremos todo el vector. Partici´ on() ∈ O ( n ). 63

Vamos a ver un ejemplo, uniendo los subconjunto de los elemento 4 y 1: Figura 10.3: Resultado implementaci´ on vector de pertenencia Vemos que todos los ‘1’ del vector han pasado a ser ‘4’, es decir, todos los elementos que esta- ban en el subconjunto donde el representante era el valor ‘1’ se han incluido en el subconjunto cuyo representante es el valor ‘4’. Implementaci´ on mediante lista de elementos Es una implementaci´ on cuya eficiencia espacial es menor (se gasta m´ as espacio). Figura 10.4: Ejemplo implementaci´ on lista de elementos Ahora almacenamos tanto el ‘vector de pertenencia’ ( P ) que contiene el representante de cada subconjunto y un campo que indica que elemento le precede, en el caso del elemento ‘0’ el valor es -1, es decir, no hay m´ as valores en el subconjunto, pero si nos vamos al elemento ‘8’ vemos que su valor es 9, es decir que el elemento ‘8’ precede al elemento ‘9’. Queremos realizar la operaci´ on unir(4,1) , como vemos en la Figura 10.4 , tanto el elemento ‘4’ como el ‘1’ son representantes de sus subconjuntos, por tanto si realizamos la uni´ on los valores que suceden a los elementos va a cambiar, quedando: Figura 10.5: Resultado implementaci´ on lista de elementos Es decir, si nos fijamos en el valor que ten´ ıa anteriormente el elemento ‘4’ que era 0, ahora vemos que ese valor es 1 (ahora del elemento 4 vamos al elemento 1), adem´ as podemos ver que el representante de los elementos ‘1’, ‘6’, ‘8’ y ‘9’ ahora es ‘4’, el representante del nuevo subconjunto. El coste de las operaciones son: encontrar() ∈ O (1), porque accedemos a la posici´ on directamente. unir() ∈ O ( n ), pero el tiempo de ejecuci´ on es menor. 64

Implementaci´ on mediante lista de elementos con longitud Esta implementaci´ on es muy parecida a la anteror, pero contiene un campo m´ as para poder evitar el peor caso. En este nuevo campo almacenamos la longitud para as´ ı evitar unir una lista larga con una corta, haciendo que siempre se una una corta a una larga. Figura 10.6: Ejemplo implementaci´ on lista de elementos con longitud Si nos fijamos en la Figura 10.6 veremos que el elemento ‘0’ su representante es el elemento ‘4’, adem´ as es el ´ ultimo elemento del subconjunto, ya que tiene el valor -1 y al ser el ´ ultimo elemento su longitud es ‘nada’ (el cuadro negro), a diferencia del elemento ‘7’ cuyo represen- tante es el elemento ‘7’, su sucesor es el elemento ‘2’ y la longitud es 3 (porque es el n´ umero de elementos del subconjunto), solamente los representantes tienen este valor asignado. Vamos a realizar la operaci´ on unir(4,1) : Figura 10.7: Resultado implementaci´ on lista de elementos con longitud Como vemos, se realizan los mismos cambios que en el ejemplo anterior ( Figura 10.5: Resul- tado implementaci´ on lista de elementos ), pero con el extra de que tenemos que cambiar la longitudes de los representantes. Al unir los subconjuntos de los elementos ‘4’ y ‘1’ y al ser el nuevo representante del nuevo subconjunto el elemento ‘1’ este cambiar´ a su longitud de 4 a 6. Porqu´ e es el elemento ‘1’ el nuevo representante y no ‘4’, esto se debe a que ahora unimos el subconjunto corto al subconjunto largo , como el subconjunto cuyo representante era ‘4’ era el corto en comparaci´ on al subconjunto con representante ‘1’ obtenemos el resultado del la Figura 10.7 . Ahora el coste de las operaciones son: encontrar() ∈ O (1), porque accedemos a la posici´ on directamente. unir() ∈ O ( n ), pero el tiempo de ejecuci´ on se reduce a la mitad. 65

Implementaci´ on mediante bosque de ´ arboles Ahora cada subconjunto pasan a ser ´ arboles (lo m´ as ancho posible). En cada posici´ on del vector vamos a encontrar el padre de dicho elemento en el ´ arbol, donde el representante del subconjunto ser´ a el nodo ra´ ız de dicho ´ arbol. Al igual que en los ejemplos anteriores el valor -1 indicar´ a la ausencia de elemento en dicho ´ arbol, en este caso, indicar´ a que ese nodo no tendr´ a padre y por ende ser´ a el representante (nodo r´ aiz). Figura 10.8: Ejempo implementaci´ on bosque de ´ arboles Ahora si queremos unir los subconjuntos cuyos representantes son 4,1, es decir realizar la operaci´ on unir(4,1) , tendr´ ıamos que unir el ´ arbol con representante ‘1’ en el ´ arbol con representante ‘4’ o viceversa: Figura 10.9: Resultado implementaci´ on bosque de ´ arboles En este caso, hemos unido el ´ arbol del subconjunto con representante ‘1’ al del representante ‘4’ ( Figura 10.9 ), es decir el segundo se une al primero. En este caso el coste de las operaciones son: encontrar() ∈ O ( n ). unir() ∈ O (1). 66

Implementaci´ on mediante bosque de ´ arboles con control de altura Sin embargo, si tenemos ´ arboles (subconjuntos) muy grandes y muy peque˜ nos puede ser muy ineficiente la operaci´ on unir() , para poder subsanar este problema encontramos la implementaci´ on mediante bosque de ´ arboles con control de altura , donde el unimos el ´ arbol m´ as bajo al m´ as alto, ya que la altura no sube (cota). Una altura inferior → mayor equilibrio del ´ arbol. Si los ´ arboles tiene la misma altura, da igual quien se una a quien. Vamos a diferenciar dos tipos de uni´ ones: Uni´ on por tama˜ no: El ´ arbol con menos nodos se convierte en el sub´ arbol del que tiene mayor n´ umero de nodos. Es una alternativa a hacerlo por azar. En este caso, lo relevante no es la altura, si no el tama˜ no de ´ arbol (como su nombre dice), adem´ as un error muy com´ un es pensar que mayor tama˜ no = ⇒ mayor altura, esto no es as´ ı debido a que un ´ arbol general puede tener muchos nodos hermanos y su altura puede seguir siendo 1. Uni´ on por altura: El ´ arbol menos alto se convierte en sub´ arbol del otro. Esta implementaci´ on es la m´ as acertada, debido a que vamos a unir el ´ arbol ‘m´ as peque˜ no’ al m´ as ‘grande’, haciendo que el ´ arbol resultante crezca en anchura. Figura 10.10: Ejemplo bosque de ´ arboles control de altura (uni´ on por altura) Ahora en el vector de pertenencia P , si el nodo padre, encontramos que el valor indica la altura del ´ arbol - 1 (en negativo), es decir, se pone − ( altura arbol − 1). Cuando la altura es 0 ponemos el valor -1, debido a que encontramos una ambig¨ uedad en el caso de -0. Ahora al unir dos ´ arboles, la operaci´ on encontrar() pasa a ser de coste O ( log n ), ya que vamos descartando las ramas donde no se encuentra dicho valor. Vamos a realizar la operaci´ on unir(4,1) : Como vemos en la Figura 10.10 el ´ arbol con representante (ra´ ız)‘4’ es mucho m´ as peque˜ no que el ´ arbol con ra´ ız ‘1’, por tanto, el primero se convertir´ a en sub´ arbol del ´ arbol con ra´ ız ‘1’, haciendo que el ´ arbol gane anchura y la altura no se vea incrementada ( Figura 10.11 ). 67

Finalmente, vemos que en el vector el contenido del elemento ‘4’ pasa a ser -2(altura del ´ arbol anterior) a 1 (su nodo padre). Si ambos ´ arboles tienen la misma altura, da Figura 10.11: Resultado bosque de ´ arboles control de altura (uni´ on por altura) igual en el orden que insertemos los sub´ arboles. Compresi´ on de caminos: Optimiza la altura de los ´ arboles. Ahora con la primera pasada sabemos el representante del nodo ‘x’ a encontrar, en la segunda pasada ya sabemos todos los nodos que pertenecen a dicho representante. Queremos realizar la operaci´ on encontrar(20) : Figura 10.12: Ejemplo bosque de ´ arboles control de altura (compresi´ on de caminos) En la primera pasada encontramos que el representante del subconjunto de 20, es el elemento ‘10’, en la segunda pasada colocamos el elemento ‘20’ como hijo izquierdo del representante (elemento ‘10’) y as´ ı sucesivamente con todos los elementos del subcon- junto. Si nos fijamos en el vector de pertenencia P , el elemento ‘18’ tiene como representante al elemento ‘10’, pero esto no se corresponde con el ´ arbol visto en la Figura 10.12 , por tanto, vamos a optimizar su altura, haciendo que el nodo con valor ‘18’ sea ahora un hijo del nodo con valor ‘10’, lo mismo con el elemento ‘14’. 68

Figura 10.13: Resultado bosque de ´ arboles control de altura (compresi´ on de caminos) Vemos que los nodos 20,14 y 18 (se˜ nalados en rojo) han cambiado de posici´ on en el ´ arbol, disminuyendo la altura e incrementando la anchura del mismo, adem´ as si nos fijamos en le vector de pertenencia los valores de elementos 20(14) ,14(18) y 18(25) han cambiado a 10. Si hiceramos la operaci´ on encontrar(16) , tanto el nodo 16, como 9 cambiar´ ıan de posici´ on siendo hijos de nodo 10 (su representante). Gracias a esto el coste de las operaciones son: encontrar() O ( log n ). unir() ∈ O (1). C´ odigo de la implementaci´ on mediante bosque de ´ arboles control de altura class Particion \{ public : Particion( int n): padre(n-1)\{\} void unir( int a, int b); int encontrar( int x) const ; private : mutable std::vector&lt; int &gt;padre; \}; void Particion::unir( int a, int b)\{ if (padre[b] &lt; padre[a])\{ padre[a] = b; \} else if (padre[a] == padre[b])\{//como aumenta la altura, le restamos 1 padre[a]--; //´ arbol resultante tiene un nivel m´ as padre[b] = a; \} \} 69
