---
title: "Árboles parcialmente ordenados"
---
import useBaseUrl from '@docusaurus/useBaseUrl';

> Fuente: <a href={useBaseUrl('/ednl/TeoriaEDNL.pdf')} data-noBrokenLinkCheck>TeoriaEDNL.pdf</a>

6. ´ Arboles parcialmente ordenados Un ´ arbol parcialmente ordenado o APO , es un ´ arbol completo (´ arbol que tiene todos sus niveles llenos menos el ´ ultimo que faltan nodos por la derecha) en el que el valor de cualquier nodo es menor o igual que el de todos sus descendientes . 6.1. Operaciones b´ asicas Cuando trabajamos con APOs podemos realizar varias operaciones como acceso al m´ ınimo valor , inserci´ on y eliminaci´ on . El m´ ınimo valor se puede obtener en un tiempo de O (1), debido a que el m´ ınimo valor siempre ser´ a la ra´ ız del propio ´ arbol. Encontramos la propiedad de completitud que implica que la altura menor posible del APO de n nodos es de h = log 2 n . Tambi´ en existe la propiedad de orden del APO que permite efectuar inserciones y eliminaciones en el mismo en un tiempo O ( h ) (en el peor caso), y en un tiempo O ( log 2 n ) en el mejor y caso promedio. 6.2. Especificaci´ on del TAD ´ arbol parcialmente ordenado En los ´ arboles parcialmente ordenados vamos a encontrar dos m´ etodos hundir() y flotar() . Constructor del ´ arbol parcialmente ordenado Precondici´ on: maxNodos &gt; 0 Postcondici´ on: Crea y devuelve un APO de tama˜ no MaxNodos vac´ ıo. Apo(size_t maxNodos); 31

Inserci´ on de elementos en el APO Precondici´ on: APO no lleno. Postcondici´ on: Inserta el elemento en su posici´ on correcta. void insertar(const T&amp; e); Eliminaci´ on de elementos en el APO Precondici´ on: APO no vac´ ıo. Postcondici´ on: Elimina la ra´ ız reordenando el APO. void suprimir(); M´ etodos observadores de un APO Obtener la ra´ ız : Precondici´ on: APO no vac´ ıo. Postcondici´ on: Devuelve el elemento que est´ a en la ra´ ız. const T&amp; cima()const; Obtener estado del ´ arbol : Postcondici´ on: Devuelve True si el ´ arbol est´ a vac´ ıo, si no, False . bool vacio()const; Obtener el padre : Precondici´ on: APO no vac´ ıo. Postcondici´ on: Devuelve el padre del nodo n. nodo padre(nodo n)const; Obtener el hijo izquierdo : Precondici´ on: APO no vac´ ıo. Postcondici´ on: Devuelve el hijo izquierdo del nodo n . nodo hIzq(nodo n)const; Obtener el hijo derecho : Precondici´ on: APO no vac´ ıo. Postcondici´ on: Devuelve el hijo derecho del nodo n . nodo hDer(nodo n)const; M´ etodos hundir y flotar de un APO Hundir un nodo : Este m´ etodo ir´ a de la mano a la hora de eliminar el nodo ra´ ız, reordenando el ´ arbol hasta que dicho nodo sea una hoja y lo podamos eliminar. Podemos tener 3 casos a la hora de eliminar la ra´ ız del APO: • Caso 1: Un nodo → Al tener un solo nodo (ra´ ız), si la eliminamos el ´ arbol quedar´ a vac´ ıo . • Caso 2: Dos nodo → Ahora tenemos tanto la ra´ ız como su hijo izquierdo, por tanto, si eliminamos la ra´ ız, su hijo izquierdo pasar´ a a ser la nueva ra´ ız del APO. 32
