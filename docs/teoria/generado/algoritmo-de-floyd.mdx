---
title: "Algoritmo de Floyd"
---
import useBaseUrl from '@docusaurus/useBaseUrl';

> Fuente: <a href={useBaseUrl('/ednl/TeoriaEDNL.pdf')} data-noBrokenLinkCheck>TeoriaEDNL.pdf</a>

for (v=0;v&lt;= n-1; v++)\{ if (!S[v] &amp;&amp; D[V] &lt;=costeMinimo)\{ costeMinimo = D[v]; w = v; \} S[w] = true; //Recalcular coste hasta cada v no incluido en S, a trav´ es de w. //Aqui optimizamos de verdad, donde se rec´ alcala los valores del vector. for (v=0; v&lt;=n-1; v++) if (!S[v])\{ tCoste Owv = suma(D[w], G[w][v]); if (Owv &lt; D[v])\{ D[v] = Owv; //Coste de ir del origen a v a trav´ es de w. P[v] = w; \} \} \} return D; \} M´ etodo Camino Este m´ etodo recibe el v´ ertice origen, un vertice cualquiera (destino) y el vector de v´ ertices y devuelve el camino. template &lt; typename tCoste &gt; typename GrafoP &lt;tCoste&gt;::tCamino camino( typename GrafoP &lt;tCoste&gt;::vertice orig, typename GrafoP &lt;tCoste&gt;::vertice v, const vector&lt; typename GrafoP &lt;tCoste&gt;::vertice&gt;&amp; P)\{ , → , → typename GrafoP &lt;tCoste&gt;::tCamino C; C.insertar(v,C.primera()); do \{ C.insertar(P[v], C.primera()); v = P[v]; \} while (v!=orig); return C; \} 9.2. Algoritmo de Floyd El algoritmo de Floyd calcula todos los caminos de coste m´ ınimo entre cada par de v´ ertices del grafo G . template &lt; typename tCoste &gt; matriz&lt;tCoste&gt; Floyd( const GrafoP&lt;tCoste&gt;&amp; G, matriz&lt; typename GrafoP &lt;tCoste&gt;::vertice&gt;&amp; P); Como salida tenemos: Matriz de costes m´ ınimos de tama˜ no n ∗ n , con n = G.numVert() . 57

Matriz de v´ ertices ( P ) de tama˜ no n ∗ n , tal que, P[i][j] es un v´ ertice intermedio, por el que pasa el camino de coste m´ ınimo i, j . El sub´ ındice A k − 1 significa la iteraci´ on en la que estamos. Siempre nos quedamos con el valor m´ ınimo. En Dijkstra asumimos que el coste m´ ınimo de ir del origen as´ ı mismo es 0. En Floyd el equivalente a lo que hacemos en Dijkstra ser´ a colocar 0 (n veces), esto se traduce en poner todo a 0 la diagonal principal de la matriz de costes m´ ınimos . 9.2.1. C´ odigo del algoritmo de Floyd Algoritmo de Floyd template &lt; typename tCoste &gt; matriz&lt;tCoste&gt; Floyd( const GrafoP&lt;tCoste&gt;&amp; G, matriz&lt; typename GrafoP &lt;tCoste&gt;::vertice&gt;&amp; P)\{ , → typedef typedef GrafoP&lt;tCoste&gt;::vertice vertice; const size_t n = G.numVert(); matriz&lt;tCoste&gt; A(n); //matriz de costes m´ ınimos //Iniciamos A y P con caminos directos entre cada par de v´ ertices P = matriz&lt;vertice&gt;(n); //creamos una matriz de vertices for (vertice i = 0; i&lt;= n-1 ; i++)\{ A[i] = G[i]; //copiamos los costes del grafo A[i][i] = 0; //diagonal principal a 0. P[i] = vector&lt;vertice&gt;(n,i); //caminos directos \} //Calcular costes minimos y caminos correspondientes //entre cualquier par de v´ ertices i, j for (vertice k = 0; k &lt;= n-1; k++) for (vertice i = 0; i &lt;= n-1; i++) for (vertice j = 0; j&lt;=n-1; j++)\{ tCoste ikj = suma(A[i][k], A[k][j]); if (ikj &lt; A[i][j])\{ //optimizaci´ on de los costes minimos A[i][j] = ikj; P[i][j] = k; \} 58

\} return A; //devolvemos la matriz de costes \} Cosas a tener en cuenta del c´ odigo: Con A[i] = G[i] → copiamos los costes del Grafo en la matriz entrante. Con A[i][i] = 0 → hacemos que el coste m´ ınimo de ir a donde estamos nosotros mismos sea 0, es decir la diagonal principal de la matriz (como hemos comentado anteriormente). Con P[i] = vector&lt;vertice&gt;(n,i) → asignamos a P[i] un vector de v´ ertices por los que pasa. La variable ikj → es la suma de los costes m´ ınmos de ir desde un v´ ertice i a otro j pasando por k , es decir, ikj = suma(A[i][k], A[k][j]) . Si la suma de estos costes es menor que el coste del grafo ‘ A[i][j] ’, es decir ( ikj &lt; A[i][j] ), podemos optimizar el coste de ir de i → j . M´ etodo Camino Este m´ etodo devuelve una lista de v´ ertices entre dos v´ ertices cualesquiera: template &lt; typename tCoste &gt; typename GrafoP &lt;tCoste&gt;::tCamino caminoAux( typename GrafoP &lt;tCoste&gt;::vertice v, typename GrafoP &lt;tCoste&gt;::vertice w, const matriz&lt; typename GrafoP &lt;tCoste&gt;::vertice&gt;&amp; P)\{ , → , → // Devuelve el camino de coste m´ ınimo entre v y w, exluidos estos, // a partir de una matriz P obtenida mediante la funci´ on Floyd(). typename GrafoP &lt;tCoste&gt;::tCamino C1, C2; typename GrafoP &lt;tCoste&gt;::vertice u; u = P[v][w]; if (u != w)\{ C1 = caminoAux&lt;tCoste&gt;(v,u,P); C1.insertar(u,C1.fin()); C2 = caminoAux&lt;tCamino(u,w,P); C1 += C2; //lista&lt;vertice&gt;::operator *=(), concatena C1 y C2. \} return C1; /7/devolvemos la lista \} template &lt; typename tCoste &gt; typename GrafoP &lt;tCoste&gt;::tCamino camino( typename GrafoP &lt;tCoste&gt;::vertice v, typename GrafoP &lt;tCoste&gt;::vertice w, const matriz&lt; typename GrafoP &lt;tCoste&gt;::vertice&gt;&amp; P)\{ , → , → // Devuelve el camino de coste m´ ınimo desde v hasta w a partir // de una matriz P obtenida mediante la funci´ on Floyd(). typename GrafoP &lt;tCoste&gt;::tCamino C; C = caminoAux&lt;tCoste&gt;(v,w,P); C.insertar(v,C.primera()); C.insertar(w,C.fin()); return C; \} 59
