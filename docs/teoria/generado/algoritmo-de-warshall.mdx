---
title: "Algoritmo de Warshall"
---
import useBaseUrl from '@docusaurus/useBaseUrl';

> Fuente: <a href={useBaseUrl('/ednl/TeoriaEDNL.pdf')} data-noBrokenLinkCheck>TeoriaEDNL.pdf</a>

Cosas a tener en cuenta del c´ odigo del m´ etodo caminoAux : El v´ ertice u es el ´ ultimo v´ ertice que se ha utilizado para optimizar el camino entre los v´ ertices v y w . Mediante C1 = caminoAux&lt;tCoste&gt;(v,u,P) → calculamos el camino desde el v´ ertice v hasta el v´ ertice u . Mediante C2 = caminoAux&lt;tCamino(u,w,P) → calculamos el camino desde el v´ ertice u hasta el v´ ertice w . Con C1 += C2 → concatenamos los caminos en la lista de v´ ertices. En este m´ etodo vemos que solamente insertamos el ´ ultimo v´ ertice por el que se ha pasado en el camino v → w , es decir, u . 9.3. Algoritmo de Warshall El algoritmo de Warshall determina si hay un camino entre cada par de v´ ertices del grafo no ponderado G . matriz&lt; bool &gt; Warshall( const Grafo&amp; G) Como salida tenemos: Una matriz booleana cuadrada de tama˜ no G.numVert() , donde la posici´ on [i][j] es true si existe al menos un camino entre los v´ ertice i y j , y es false si no existe un camino entre ese par de v´ ertices. 9.3.1. C´ odigo del algoritmo de Warshall Algoritmo de Warshall matriz&lt; bool &gt; Warshall( const Grafo&amp; G)\{ typedef Grafo::vertice vertice; const size_t n = G.numVert(); matriz&lt; bool &gt;A(n); //creamos la matriz de booleanos. //Inicializamos A con la matriz de adyacencia. for (vertice i = 0; i&lt;=n-1; i++)\{ A[i] = G[i]; A[i][i] = true; \} // Comprobar camino entre cada par de v´ ertices i, j // a trav´ es de cada v´ ertice k. for (vertice k = 0; k &lt;= n-1; k++) for (vertice i = 0; i &lt;= n-1; i++) for (vertice j = 0; j &lt;= n-1; j++)\{ if (!A[i][j])\{ A[i][j] = A[i][k] &amp;&amp; A[k][j]; \} \} 60

return A; \} Cosas a tener en cuenta del c´ odigo: Con A[i][i] = true → indicamos que hay camino, es decir, siempre hay camino entre un v´ etice cualquiera y s´ ı mismo. Con !A[i][j] → comprobamos si hay o no camino entre dicho par de v´ ertice (si es true o false ). A[i][j] = A[i][k] &amp;&amp; A[k][j] → se ejecuta en el caso de que lo anterior fuera false (no hay camino), donde comprobamos si hay camino entre i, j , pero pasando por otro v´ ertice cualquiera k . 61
