---
title: "Práctica 3: Árboles Generales"
---
import useBaseUrl from '@docusaurus/useBaseUrl';

> Fuente: <a href={useBaseUrl('/ednl/PracticasEDNL.pdf')} data-noBrokenLinkCheck>PracticasEDNL.pdf</a>

Pr´ actica 3: ´ Arboles Generales En esta pr´ actica vamos a ver como trabajar con el TAD Agen con sus dos representaciones (enlazada y vectorial). Como cabeceras de la pr´ actica vamos a tener: #include &lt;iostream&gt; #include "agen.h" //Especificaci´ on e implementaci´ on de los m´ etodos del TAD. #include "agen_E-S.h" Ejercicio 1: Implementa un subprograma que dado un ´ arbol general nos calcule su grado. Para saber cual es el grado de un ´ arbol este es el m´ aximo de los grados de los nodos que componen al ´ arbol. Por tanto, para saber cuales el grado del Agen, tenemos que saber cual es el grado de cada nodo que lo compone y quedarnos con el m´ aximo. template &lt; typename T &gt; size_t gradoAgen( const Agen&lt;T&gt; &amp;A)\{ return gradoAgen_rec(A.raiz(),A); \} template &lt; typename T &gt; size_t gradoAgen_rec( typename Agen &lt;T&gt;::nodo n, const Agen&lt;T&gt; &amp;A)\{ , → if (n == Agen&lt;T&gt;::NODO_NULO)\{ return 0; //el grado de un nodo nulo (no existe nodo) es 0 \} else \{ size_t gradoMax = 0; //grado del ´ arbol size_t nHijos = 0; //grado del nodo typename Agen &lt;T&gt;::nodo aux = A.hijoIzqdo(n); //Calculamos el grado del ´ arbol. while (aux != Agen&lt;T&gt;::NODO_NULO)\{ nHijos++; gradoMax = std::max(gradoMax,gradoAgen_rec(aux,A)); aux = A.hermDrcho(aux); \} return std::max(nHijos,gradoMax); \} \} Ejercicio 2: Implementa un subprograma que dados un ´ arbol y un nodo dentro de dicho ´ arbol determine la profundidad de ´ este nodo en el ´ arbol. Para calcular la profundidad de un nodo cualquiera en un ´ arbol general, este se va a calcular de la misma forma que en los ´ arboles binarios, ya que lo que tenemos que hacer es partir del nodo en cuesti´ on e ir llamando al padre del mismo hasta llegar a la raiz. template &lt; typename T &gt; size_t profundidadAgen_rec( typename Agen &lt;T&gt;::nodo n, const Agen&lt;T&gt; &amp;A)\{ , → if (n == A.raiz()) return 0; else return 1+profundidadAgen_rec(A.padre(n),A); \} 10

Ejercicio 3: Se define el desequilibrio de un ´ arbol general como la m´ axima diferencia entre las alturas de los sub´ arboles m´ as bajo y m´ as alto de cada nivel. Implementa un subprograma que calcule el grado de desequilibrio de un ´ arbol general. Vamos a suponer que tenemos un m´ etodo llamado alturaAgen() , el cual nos har´ a falta para poder calcular el desequilibrio de un ´ arbol general (seg´ un la definici´ o de desequilibrio dada). Para calcular el desequilibrio vamos a recorrer todos los hermanos del hijo del nodo dado, y vamos calculando el desequilibrio m´ aximo mediante la funci´ on std::max() . template &lt; typename T &gt; size_t desequilibrioAgen( const Agen&lt;T&gt; &amp;A)\{ return profundidadAgen_rec(A.raiz(),A); \} template &lt; typename T &gt; size_t desequilibrioAgen_rec( typename Agen &lt;T&gt;::nodo n, const Agen&lt;T&gt; &amp;A)\{ , → if (n == Agen&lt;T&gt;::NODO_NULO)\{ return 0; \} else \{ size_t maxDesequilibrio = 0; typename Agen &lt;T&gt;::nodo aux = A.hijoIzqdo(n); while (aux != Agen&lt;T&gt;::NODO_NULO)\{ maxDesequilibrio = std::max(maxDesequilibrio, std::abs(alturaAgen_rec(aux,A) - alturaAgen(A.hermDrcho(aux),A))); , → aux = A.hermDrcho(aux); \} return maxDesequilibrio; \} \} Ejercicio 4: Dado un ´ arbol general de enteros A y un entero x, implementa un subprograma que realice la poda de A a partir de x. Se asume que no hay elementos repetidos en A. Una poda significa que apartir de dicho nodo tenemos que eliminar todos sus desecendientes y a ´ el mismo. Para ello, primero tenemos que realizar la b´ usqueda del elemento ‘x’ en el Agen y a partir de su posici´ on eliminar todos los desecendientes. //M´ etodo buscar un nodo a partir de su contenido template &lt; typename T &gt; typename Agen &lt;T&gt;::nodo buscar(T e, Agen&lt;T&gt; &amp;A)\{ return buscar_rec(e,A.raiz(),A); \} template &lt; typename T &gt; typename Agen &lt;T&gt;::nodo buscar_rec(T e, typename Agen &lt;T&gt;::nodo n, Agen&lt;T&gt; &amp;A)\{ , → if (n==Agen&lt;T&gt;::NODO_NULO)\{ return Agen&lt;T&gt;::NODO_NULO; \} else if (A.elemento(n) == e)\{ //Lo hemos encontrado, lo devolvemos return n; \} else \{ 11

//Vamos a buscar en sus hermanos typename Agen &lt;T&gt;::nodo aux = A.hijoIzqdo(n); while (aux != Agen&lt;T&gt;::NODO_NULO)\{ typename Agen &lt;T&gt;::nodo aux2 = buscar_rec(e,aux,A); if (aux2 != Agen&lt;T&gt;::NODO_NULO)\{ return aux2; \} aux = A.hermDrcho(aux); \} return Agen&lt;T&gt;::NODO_NULO; //no existe entre los hermanos \} \} template &lt; typename T &gt; void podaAgen(T x, Agen&lt;T&gt; &amp;A)\{ typename Agen &lt;T&gt;::nodo n = buscar(x,A); return podaAgen_rec(n,A); \} template &lt; typename T &gt; void podaAgen_rec( typename Agen &lt;T&gt;::nodo n, Agen&lt;T&gt;: &amp;A)\{ if (n != Agen&lt;T&gt;::NODO_NULO)\{ //Mientras que n tenga hijos, podamos while (A.hijoIzqdo(n)!= Agen&lt;T&gt;::NODO_NULO)\{ //Realizamos la llamada recursiva con el hijo (poda del sub´ arbol del hijo) podaAgen_rec(A.hijoIzqdo(n),A); A.eliminarHijoIzqdo(n); //eliminamos al propio hijo \} \} \} 12
